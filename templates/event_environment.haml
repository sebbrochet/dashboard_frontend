%h1= environment.name

%table.table
   %thead
      %tr
         %th Date
         %th Subject
         %th Message
      %tbody
         :each event in events
            %tr
               %td= event.date
               %td= event.subject
               %td
                  %pre= event.message
